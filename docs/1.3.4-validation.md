# 1.3.4 acceptance

The installed Linux build completed one natural automatic rollover on September
5, 2026. Times below are America/Toronto (EDT).

- 16:19:53: one rollover reservation using `gpt-5.6-luna / low`.
- The same attempt progressed through launch attempted and possibly sent.
- 16:20:31: post-trigger observations verified the fixed reset at 21:19:51.
- Four samples over 32 seconds agreed on the reset timestamp. Recent starts
  reports Confirmed; the provider is Ready and compatibility remains passed.
- The journal contains one attempt, with no duplicate reservation or retry.

The run closes the remaining Linux acceptance gap. A focused inspection found
no runtime defect requiring a patch. Raw installed state and journal snapshots
remain local; no account data or runtime identifiers are included here.

The earlier failed compatibility state recovered through the inherited
read-only Recheck action without sending a model turn. The cross-filesystem
XDG migration regression preserves settings and trigger reservations. Trigger
selection, standard service, weekly protection, and idempotency code are
unchanged for this launch.

The release uses Windows and Linux packages from the verification workflow on
released main. That workflow runs both suites and the Windows fresh-install,
upgrade-chain, and broken-registration scenarios. Keep the Windows installer
and checksum filenames unchanged for existing installations.
